apiVersion: v1
kind: Service
metadata:
  name: nt548-lab3
spec:
  selector:
    app: nt548-lab3
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30000
  type: NodePort
